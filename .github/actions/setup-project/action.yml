name: Setup project
description: Set up Bun (cached) and install dependencies. Assumes the repo has already been checked out.

runs:
  using: composite
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: '1.4.0'

    - name: Install dependencies
      shell: bash
      run: bun install --frozen-lockfile
